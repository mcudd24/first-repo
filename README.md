# first-repo
I am cool

## Example Video

`create_video.py` generates a simple animated sine wave video using matplotlib.

**Requirements:** `matplotlib`, `numpy`, and `ffmpeg` installed.

```bash
pip install matplotlib numpy
python create_video.py
# outputs example_video.mp4
```
